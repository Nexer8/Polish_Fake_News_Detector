const Routes = {
  editorReport: '/editor/report/:id',
  editorReports: '/editor/reports',
  editorLogin: '/editor/login',
  result: '/result/:id',
  resultReport: '/result/:id/report',
  statementVerifier: '/',
  wildcard: '*',
};

export const domain = 'http://weryfikator.pl';
export default Routes;
