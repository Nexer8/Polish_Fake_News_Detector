import React from 'react';
import { SidebarTemplate } from 'templates/SidebarTemplate';

interface Props {
  // TODO: define props here
}

export const EditorReports: React.FC<Props> = () => {
  return (
    <SidebarTemplate sidebar={<div>TODO: Filters</div>}>
      EditorReports
    </SidebarTemplate>
  );
};
